## 🧪 Exercise 1.3 – Practice Task 2

### 🎯 Objective

Use a `for` loop to display the top three scores from a list of test scores.

### 🧠 Logic Used

- Create a list of scores.
- Sort the list in descending order using `sort(reverse=True)`.
- Use a `for` loop to print the first three scores.

### 📂 Folder Contents

- `top_scores.py`: Python script for the task.
  screenshot: scores.png
  creating the list.
  sorting the list.
  printing top 3 scores.

### 🚀 How to Run

```bash
python top_scores.py
```
