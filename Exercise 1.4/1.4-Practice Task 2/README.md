# Practice Task 2 – Binary Files with Pickle

This folder contains a screenshot demonstrating the successful completion of Practice Task 2 from Exercise 1.4 of the Code First Python course.

## ✅ Task Summary

- Imported the `pickle` module.
- Created a dictionary representing a simple tea recipe.
- Stored the dictionary in a binary file named `recipe_binary.bin` using `pickle.dump()`.
- Loaded the data back using `pickle.load()` and displayed the recipe with readable formatting.

## 📸 Screenshot

- `Load_Recipe_data_Binary.png`: Shows the final output where the recipe is loaded from the binary file and printed clearly.

## 💡 Notes

The task was completed using IPython to ensure proper execution of Python syntax and file handling.
